import { User } from './User';
import { Category } from './Category';
import { Transaction } from './Transaction';

const entities = [
    User, 
    Category,
    Transaction
];

export { 
    User, 
    Category,
    Transaction, 
    entities 
};
